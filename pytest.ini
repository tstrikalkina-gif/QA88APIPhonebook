[pytest]
markers =
    smoke: Quick smoke tests
    auth: Authorization tests
    negative: Negative tests

pythonpath = .
testpaths = tests
addopts =
    -v
    -s
    --html=reports/report.html
    --self-contained-html